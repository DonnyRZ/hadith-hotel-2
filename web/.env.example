# Copy to .env and adjust if needed
# Start database: from repo root → docker compose up -d db

DATABASE_URL="postgresql://hadith2:hadith2@localhost:5435/hadith_hotel_2?schema=public"

# Guest registration → Google Sheets sync (optional locally; sync is skipped
# silently if unset). Get these from the service account JSON key.
# GOOGLE_SERVICE_ACCOUNT_EMAIL="xxx@yyy.iam.gserviceaccount.com"
# GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GOOGLE_SHEETS_SPREADSHEET_ID="..."
# GOOGLE_SHEETS_SHEET_NAME="Sheet1"
